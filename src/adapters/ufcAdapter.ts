/**
 * UFC Adapter — Placeholder for future API integration.
 */

import type { UFCFighter } from "@/data/ufc/mockData";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function mapApiFighterToUFCFighter(raw: any): UFCFighter {
  return raw as UFCFighter;
}
